Write a specification for: $ARGUMENTS

Follow the enumeration process from CLAUDE.md:
1. What happens today? Trace the actual mechanism.
2. Enumerate all cases - every input variant, output variant, cross-product.
3. Identify coupling with other features.

Output format:
- Overview (2-3 sentences)
- Inputs (what triggers this feature)
- Outputs (what changes in the system)
- Cases (table or list of all scenarios)
- Edge cases and error handling
- Open questions (if any)

Save to: docs/specs/<feature-name>.md
